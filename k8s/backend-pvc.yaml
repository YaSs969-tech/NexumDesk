apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexumdesk-sqlite-pvc
  namespace: nexumdesk
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
